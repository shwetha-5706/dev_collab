# Frontend (optional — defaults to same-origin /api via Vite proxy)
# VITE_API_URL=http://localhost:3001/api
# VITE_SOCKET_URL=http://localhost:3001

# Backend
# PORT=3001
# JWT_SECRET=change-me-in-production

# AI (optional — without this key, DevCollab uses smart local AI fallbacks)
# OPENAI_API_KEY=sk-...
# OPENAI_MODEL=gpt-4o-mini
